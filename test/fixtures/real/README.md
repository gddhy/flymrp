# Real binary fixtures

No real `start.mr` / `.mrp` / `魔塔II.jar` is present in this workspace or in the rxgj source tree.

**No real binary fixture available.**

Do not add forged “real app” binaries here. If a user supplies an unprotected MRP/MR, record SHA-256, magic (`MRPG`/`MRPF`), file list, `start.mr` presence, and EXT presence — and do not modify the original file.
