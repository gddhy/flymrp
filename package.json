{
  "name": "flymrp",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "description": "Browser-native Mythroad MRP runtime — Stage 4 EXT ABI",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "tsx bench/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
