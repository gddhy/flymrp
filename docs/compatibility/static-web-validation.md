# 2026-09-07 静态网页验证

本轮增加 TinySynth GM MIDI 播放器、保留轻量方波选项，默认使用 GM；网络拦截仅在运行时后台处理，玩家界面不显示 JSON 或请求日志。界面参考本机 `myjump/javasrc/style/index.css` 的灰白卡片、蓝色主按钮和圆角样式。

- `./build.sh` 完整执行成功，包括锁定依赖安装、TypeScript 检查、Vite 构建和资源复制。输出为根目录 `dist/`。
- 用户 `mythroad_res` 中的 15,630 个非隐藏常规文件（391,344,798 字节）全部复制，逐文件 SHA-256 与源目录一致。预加载索引包含 14,770 个资源、728 个分组，排除已知存档/注册文件；原文件仍保留在发布目录中。
- 以 Python 静态文件服务器打开 `http://127.0.0.1:5175/dist/`，确认支持子目录资源路径。生产页面不调用开发游戏库 API。
- 用网页文件选择器打开《神兽传说3-v1001-240x320.mrp》，完成内存检测、启用声音、标题、武器选择、跳过叙述、连续对话，进入含角色与伤害数值的战斗场景。TinySynth 与轻量方波可互相切换，网页无声音错误，控制台没有 warning/error。这是入口和战斗流程验证，不代表游戏通关。
- 广告脚本已按用户更正替换为 Google AdSense `ca-pub-3251239894110421`，移除原 Google Ads 标签及其初始化代码。源码和重新构建后的 `index.html` 均检查新脚本及 `crossorigin="anonymous"`；未验证账户审核或广告投放结果。
- 全量自动测试：118 个测试文件通过、1 个跳过；675 项通过、1 项跳过。新增 MIDI 解析（tempo、running status、SMPTE、损坏输入）及音频停止/异步解码竞态测试。

104 文件批测结果见 `collection-resources-results.json`。本轮没有运行时指令异常，但 10 个无输入画面响应、2 个退出、92 个待审核场景，`allPassed` 仍为 false。不能把标题或剧情变化直接作为可玩通过证据。
