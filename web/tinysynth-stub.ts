/** KaiOS 2.x uses HTML Audio; this stub keeps the desktop import graph valid. */
export default class WebAudioTinySynth {
  constructor() {
    throw new Error("TinySynth is not available on KaiOS");
  }
}
